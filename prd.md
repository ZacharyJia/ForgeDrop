### 核心目标
构建一个轻量、自托管的部署工具，帮助自动化 Java 应用（JAR 包）/ 其他应用 的部署流程，核心是“CI 只负责构建 JAR，部署工具只负责接收 JAR 并运行它”。

### 主要需求列表

1. **生产环境部署（主分支）**
   - 支持从外部 CI（Forgejo）接收新构建的 JAR 文件。
   - JAR 文件需要版本化存储（每个部署一个独立版本，不覆盖旧文件）。
   - 支持快速切换到任意历史版本（回滚）。
   - 部署后自动重启生产容器/服务，使用最新的 JAR。
   - 生产环境始终运行一个固定的 Docker 镜像（例如 openjdk:17-jre-slim），只通过挂载的方式使用 JAR 文件（JAR 不打包进镜像）。

2. **PR 预览环境（Preview Deployments）**
   - 当 Pull Request 打开、更新（synchronize）或重新打开时：
     - 自动为该 PR 创建一个独立的预览实例。
     - 每个预览实例使用独立的 JAR 文件（从 CI 接收）。
     - 提供可访问的 URL（最好是类似 pr-123.yourdomain.com 的子域名形式）。
   - 当 Pull Request 关闭或合并时：
     - 自动删除对应的预览实例和相关文件（清理干净，不留垃圾）。
   - 预览环境之间相互隔离，不影响生产。

3. **触发方式**
   - 通过 Forgejo 的 webhook 接收事件（push、pull_request opened/synchronize/closed 等）。
   - CI（Forgejo Actions）负责：
     - 构建 JAR
     - 把 JAR 上传到服务器的指定位置（生产或 PR 专用路径）
   - 部署工具只负责：
     - 接收 webhook 事件
     - 根据事件做容器创建/更新/删除/重启
     - 处理版本管理和清理

4. **其他重要要求**
   - 不负责任何构建过程（不拉代码、不编译、不运行 mvn/docker build）。
   - 支持多版本管理 + 简单回滚（生产环境能快速切回旧版本）。
   - 资源占用低、部署简单、可运行在单台 VPS 上。
   - 安全性基本要求：验证 webhook 来源、容器非 root 运行、避免权限过大。
   - 可选但加分：支持通知（PR 评论中放预览 URL）、日志查看、简单状态查询。

### 非需求（明确不要的）
- 不需要内置 Git 集成、自动拉代码、自动构建。
- 不需要复杂的 UI dashboard（CLI 或极简 web 页面即可）。
- 不需要数据库/服务一键部署、备份、监控等 PaaS 功能。
- 不需要原生支持 GitHub/GitLab（只针对 Forgejo）。

一句话概括你的需求：
“一个极简的 webhook 驱动部署服务：接收 Forgejo 推送的 JAR 文件，管理版本，自动运行/更新生产容器，并在 PR 时创建/销毁隔离的预览容器。”
